.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install localiza

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv localiza
    $ pip install localiza
